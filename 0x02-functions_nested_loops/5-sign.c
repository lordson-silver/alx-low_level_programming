#include <stdio.h>
#include "main.h"

/**
 * print_sign - function that checks for lower case
 * @n: character to be checked in ASCII
 * Return: 1 if true, 0 if false
 */

int print_sign(int n)
{
	if (n > 0)
		return (1);
	else if (n = 0)
		return (0);
	else
		return (-1);
}
